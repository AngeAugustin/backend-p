# Neon (production) — use pooler URL + pgbouncer for Prisma
DATABASE_URL="postgresql://neondb_owner:YOUR_PASSWORD@ep-falling-wildflower-au3ypauj-pooler.c-10.us-east-1.aws.neon.tech/neondb?sslmode=require&pgbouncer=true"

# Strong random secret (openssl rand -hex 32)
AUTH_SECRET="replace-with-a-long-random-secret"

ADMIN_EMAIL="admin@portfolio.local"
ADMIN_PASSWORD="admin123"

# Set AFTER both apps are deployed (then redeploy backend)
FRONTEND_URL="https://YOUR-FRONTEND.vercel.app"
NEXT_PUBLIC_APP_URL="https://YOUR-BACKEND.vercel.app"
